# Server Configuration
server:
  host: "0.0.0.0"
  port: 50051
  max_connections: 20

# Executor Configuration
executor:
  timeout: 30s
  shell: "/bin/bash"

# Logging Configuration
logging:
  level: "info"
  format: "text"
